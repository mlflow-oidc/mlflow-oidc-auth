export enum AdminPageRoutesEnum {
  PERMISSIONS = 'permissions',
  USER = 'user',
  EXPERIMENT = 'experiment',
  MODEL = 'model',
  GROUP = 'group',
  USERS = 'users',
  EXPERIMENTS = 'experiments',
  MODELS = 'models',
  GROUPS = 'groups',
  PROMPT = 'prompt',
  PROMPTS = 'prompts',
}
